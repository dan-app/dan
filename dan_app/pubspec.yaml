name: dan_app

publish_to: 'none'

version: 0.0.0+0

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  lint: ^1.5.3

flutter:
  uses-material-design: true
